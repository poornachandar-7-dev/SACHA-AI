"""
SACHA — Speech-to-text module.

NOT IMPLEMENTED YET — v1 is text-only.

Planned approach (future):
- Use faster-whisper for local, offline transcription.
- transcribe_audio(audio_bytes) -> str
"""

def transcribe_audio(audio_bytes: bytes) -> str:
    raise NotImplementedError("Speech-to-text is not implemented yet.")
