# Personality history

This folder stores past versions of `SOUL.md` so the user can revert.

Each saved snapshot is timestamped, e.g. `2026-07-30T11-48-00.md`, and is
just a copy of `../SOUL.md` at that point in time. Snapshots are written by
the personality loader whenever the user saves changes from the HUD.